import { FormCreate } from "./FormCreate";

export const Footer = () => {

    return (
        <footer className='flex justify-around items-center pb-3 border-t dark:border-gray-50 border-gray-900'>
            <FormCreate />
        </footer>
    );
}